#include "main.h"

/**
 * _islower - prints lowecase alphabets
 * return 1 or zero 
 * due to the if else conditional
 */
int _islower(int n)
{
if (n >= 'a' && a <= 'z')
	return (1);
else
	return(0);
}
