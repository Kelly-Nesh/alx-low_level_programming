#include <stdio.h>

/**
 * main - the function to be executed
 * declare an int
 * loops and print output
 * Return:zero if successful else non-zero
 */

int main(void)
{
int no;
for (no = 0; no < 10; no++)
	printf("%d", no);
printf("\n");
return (0);
}
